int fib(int n, int n)
{
	if (n == 0) {
		return 0;
	}
	if (n == 1) {
		return 1;
	}
	
	return fib(n - 1) + fib(n - 2);
}

int main(int argc, char* argv)
{
  int x, t;
  int *y;
  x = y * y - z + b;
  
  a = 2;
  y = malloc(sizeof(int) * fib(10));
  t = 0;
  
  while(t < 5) {
	y[t] = t;
	t = t + 1;
  }
  
  return x;
}